extend ../base

block content
        if doc.forms
                ul.collection                                       
                        //- +new-question(doc.form.questions, doc.form.questions.length)
                        each form, index in doc.forms
                                li
                                        a.collection-item(href='/form/' + form._id)= form.title
                                        .right
                                                span.action.delete
                                                        i.material-icons delete
                                                        span Delete
        form(action="", method="post")
                input(type="text", name="title", placeholder="Enter form title")    
                br       
                //- input(type="text", name="questions[0].title")
                //- br
                //- input(type="text", name="questions[1].title")
                //- br           
                input(type="submit", value="Save")